apiVersion: v1
kind: Service
metadata:
  name: argocd-server
  labels:
    app: argocd-server
  managedFields:
  namespace: argocd
spec:
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  selector:
    app.kubernetes.io/name: argocd-server