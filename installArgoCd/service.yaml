apiVersion: v1
kind: Service
metadata:
  name: argocd-server
  labels:
    app: argocd-server
  managedFields:
  namespace: argocd
spec:
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  #selector:
  # app.kubernetes.io/name: argocd-server